<div>
  <div>
    <img src= "<%= @profile_own.user_profile_pic %>" class="pfp_other">
    <h1>
      <%=@profile_own.username%>
    </h1>
  </div>
</div>

<hr>
<div>
  <div>
    <table border="1">
      <tr>

        <th>
          Caption
        </th>

        <th>
          Image
        </th>

        <th>
          Location
        </th>

        <th>
          Likes count
        </th>

        <th>
          Comments count
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        Show details
        </th>
      </tr>

      <% @pics.each do |a_photo| %>
      <tr>

        <td>
          <%= a_photo.caption %>
        </td>

        <td>
          <img src= "<%= a_photo.image %>" class="img-responsive">
        </td>

        <td>
          <%= a_photo.location %>
        </td>

        <td>
          <%= a_photo.likes_count %>
        </td>

        <td>
          <%= a_photo.comments_count %>
        </td>

        <td>
          <%= time_ago_in_words(a_photo.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(a_photo.updated_at) %> ago
        </td>
        <td>
        <form action ="/photos/<%= a_photo.id%>">
          <input id="photo_owner_id" type= "hidden" value=<%=a_photo.owner_id%> name="photo_owner_id">
          <a href = "/photos/<%= a_photo.id%>">
        show details </a>
        </td>
        </form>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<hr>
