<div>
  <div>
    <h1>
      Outfits of the day
    </h1>
  </div>
</div>

<hr>

<div>
  <div>
 <a href="/add_photo">
    <i class="fa-thin fa-plus" alt="Add a new fit"> </i>
 

      <button>
        add a new fit</button>
    </a>
    </form>
  </div>
</div>

<hr>
<div>
  <div>
    <table border="1">
      <tr>
        <th>
          Caption
        </th>

        <th>
          Image
        </th>

        <th>
          Owner
        </th>
       
      </tr>

      <% @timely_fits.each do |a_fit| %>
      <% if MootsRequest.where(:sender_id=>a_fit.user_id) == nil%>

      <% else %>
      <tr>
  
        <td>
          <%= a_fit.caption %>
        </td>

        <td>
          <a href="/my_closets/<%= a_fit.id %>">
          <img src= "<%= a_fit.user.user_profile_pic%>" class="img-responsive">
        </td>
         <% if session.fetch(:user_id) == a_fit.user_id %>
        <td>
        <a href="/myfits">
          <%=a_fit.user.username%></a>
        </td>
        <%else%>
        <td>
          <a href="/profile/<%= a_fit.user_id %>">
          <%=a_fit.user.username%>
          </a>
        </td>
      <%end%>
      
        <%end%>
      </tr>
      <% end %>
      
    </table>
  </div>
</div>

<hr>
